# Инструкция по проверке работы MongoDB

## Быстрая проверка подключения

### 1. Установите зависимости

```bash
cd backend
npm install
```

### 2. Настройте переменные окружения

Создайте файл `backend/.env` на основе `backend/env.example`:

```env
JWT_SECRET=your-secret-key-here-minimum-32-characters
PORT=3001
FRONTEND_URL=http://localhost:3000
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/migration_system?retryWrites=true&w=majority
MONGODB_DB_NAME=migration_system
```

⚠️ **Важно**: Замените `username`, `password` и `cluster0.xxxxx` на ваши реальные данные из MongoDB Atlas.

### 3. Запустите тест подключения

```bash
cd backend
npm run test-db
```

### Что проверяет тест:

1. ✅ Подключение к MongoDB
2. ✅ Список существующих коллекций
3. ✅ Создание индексов для базы данных
4. ✅ Корректное отключение от базы данных

### Ожидаемый результат:

Если всё работает правильно, вы увидите:

```
🔍 Проверка подключения к MongoDB...

1️⃣ Подключение к базе данных...
   ✅ Подключение успешно!

2️⃣ Список коллекций:
   📝 Коллекций пока нет (это нормально для новой базы)

3️⃣ Инициализация базы данных...
   ✅ Индексы созданы!

4️⃣ Проверка коллекций после инициализации:
   - people
   - documents
   - users

✅ Все тесты пройдены успешно!
💡 База данных готова к работе
```

## Проверка работы сервера

### 1. Запустите бэкенд сервер

```bash
cd backend
npm run dev
```

### 2. Проверьте health endpoint

В другом терминале или в браузере:

```bash
curl http://localhost:3001/api/health
```

Ожидаемый ответ: `{"status":"ok"}`

### 3. Запустите фронтенд

```bash
cd frontend
npm install
npm run dev
```

### 4. Проверьте регистрацию пользователя

1. Откройте http://localhost:3000
2. Зарегистрируйте нового пользователя
3. Войдите в систему
4. Создайте нового человека
5. Загрузите документ

## Частые проблемы и решения

### ❌ Ошибка: "MONGODB_URI is required but was not provided"

**Решение**: Убедитесь, что файл `backend/.env` существует и содержит правильную переменную `MONGODB_URI`.

### ❌ Ошибка: "MongoServerError: Authentication failed"

**Решение**: 
- Проверьте правильность username и password в строке подключения
- Убедитесь, что пользователь создан в MongoDB Atlas (Database Access)

### ❌ Ошибка: "MongoServerError: connection timed out"

**Решение**:
- Проверьте настройки Network Access в MongoDB Atlas
- Добавьте ваш IP адрес или `0.0.0.0/0` для тестирования

### ❌ Ошибка: "Cannot find module 'mongodb'"

**Решение**: 
```bash
cd backend
npm install
```

## Проверка данных в MongoDB Atlas

1. Войдите в MongoDB Atlas
2. Перейдите в "Database" → "Browse Collections"
3. Вы должны увидеть три коллекции:
   - `people` - информация о людях
   - `documents` - загруженные документы
   - `users` - зарегистрированные пользователи

## Автоматизированная проверка

Для более полной проверки можно использовать:

```bash
cd backend
npm run test-db && npm run dev
```

Это запустит тест подключения и затем сервер.

