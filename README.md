# Система управления мигрантами

Веб-сервис для управления данными мигрантов с возможностью загрузки документов и управления статусами.

## 🚀 Деплой на Vercel

Проект готов к деплою на Vercel. Подробная инструкция находится в файле [DEPLOY.md](./DEPLOY.md).

**Быстрый старт:**
1. Загрузите проект на GitHub
2. Перейдите на https://vercel.com
3. Импортируйте репозиторий
4. Добавьте переменные окружения: `JWT_SECRET` и `FRONTEND_URL`
5. Нажмите "Deploy"

⚠️ **Важно**: 
- Для продакшена обязательно настройте PostgreSQL (рекомендуется Neon PostgreSQL, см. [NEON_SETUP.md](./NEON_SETUP.md))
- **Временное хранилище файлов**: Текущая реализация использует `/tmp/uploads`, что НЕ подходит для продакшена - файлы удаляются между запросами. Обязательно мигрируйте на внешнее хранилище (Vercel Blob Storage, AWS S3, Cloudinary) перед использованием в продакшене.

💡 **Рекомендация**: Для Vercel лучше всего подходит **Neon PostgreSQL** — см. [NEON_SETUP.md](./NEON_SETUP.md)

## 🔒 Безопасность

Система включает полную защиту данных:
- **Аутентификация JWT** - защищенные токены доступа
- **Хеширование паролей** - bcrypt для безопасного хранения
- **Rate Limiting** - защита от перегрузки запросами
- **Защищенные маршруты** - все API endpoints требуют авторизации
- **Защищенные файлы** - доступ к документам только для авторизованных пользователей

## Возможности

- ✅ Создание и редактирование профилей людей
- ✅ Загрузка и просмотр документов (с просмотром изображений)
- ✅ Управление статусами (Новый, Ожидает, В обработке, Одобрен, Отклонен)
- ✅ Поиск по имени, фамилии, паспорту или email
- ✅ Удаление людей и документов
- ✅ Полноэкранный просмотр изображений
- ✅ Система входа и регистрации

## Установка

1. Установите зависимости для всех частей проекта:
```bash
npm run install:all
```

2. Настройте переменные окружения (для продакшена обязательно!):
```bash
cd backend
cp .env.example .env
```

Отредактируйте `backend/.env` и укажите:
- `JWT_SECRET` - длинный случайный ключ для JWT токенов (минимум 32 символа)
- `DATABASE_URL` - строка подключения к PostgreSQL (см. [NEON_SETUP.md](./NEON_SETUP.md) или [SUPABASE_SETUP.md](./SUPABASE_SETUP.md))
- `PORT` - порт для бэкенда (по умолчанию 3001)
- `FRONTEND_URL` - URL фронтенда (по умолчанию http://localhost:3000)

## Запуск

### Запуск всего проекта (бэкенд + фронтенд)
```bash
npm run dev
```

### Запуск отдельно

Бэкенд (порт 3001):
```bash
npm run dev:backend
```

Фронтенд (порт 3000):
```bash
npm run dev:frontend
```

## Использование

1. Откройте браузер и перейдите на http://localhost:3000
2. **Создайте аккаунт** - зарегистрируйтесь с именем пользователя и паролем
3. После входа нажмите "Создать человека" для добавления нового профиля
4. Заполните обязательные поля (Имя, Фамилия, Статус)
5. Для загрузки документов нажмите "Показать документы" в карточке человека
6. **Изображения** можно просматривать прямо на сайте - кликните на превью для полноэкранного просмотра
7. Используйте поиск для быстрого нахождения людей
8. Изменяйте статусы прямо из карточек людей

## Структура проекта

```
├── backend/                    # Express сервер (TypeScript)
│   ├── src/
│   │   ├── db/                 # База данных (JSON файлы)
│   │   ├── middleware/         # Middleware (auth, rate limiting)
│   │   ├── routes/             # API маршруты
│   │   └── index.ts            # Точка входа сервера
│   ├── uploads/                # Загруженные документы
│   └── .env                    # Переменные окружения (не в git)
├── frontend/                   # React приложение (TypeScript + Vite)
│   └── src/
│       ├── components/         # React компоненты
│       └── services/           # API клиент и auth
└── package.json                # Корневой package.json
```

## Важные замечания

⚠️ **Для продакшена обязательно:**
- Измените `JWT_SECRET` на длинный случайный ключ (минимум 32 символа)
- Настройте PostgreSQL (рекомендуется Neon PostgreSQL)
- **ВРЕМЕННОЕ ХРАНИЛИЩЕ ФАЙЛОВ**: Текущая реализация НЕ подходит для продакшена - файлы сохраняются в `/tmp/uploads` и удаляются между запросами. Обязательно мигрируйте на внешнее хранилище (Vercel Blob Storage, AWS S3, Cloudinary) перед использованием
- Настройте регулярное резервное копирование данных
- Настройте правильный CORS (FRONTEND_URL)
- Добавьте логирование действий пользователей
