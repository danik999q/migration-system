# Быстрый деплой на Vercel

## Шаг 1: Подготовка MongoDB

1. Создайте бесплатный аккаунт на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте кластер (бесплатный M0)
3. Настройте доступ:
   - **Network Access** → Добавьте `0.0.0.0/0` (разрешить доступ отовсюду)
   - **Database Access** → Создайте пользователя (username и password)
4. Получите строку подключения:
   - **Database** → **Connect** → **Connect your application**
   - Скопируйте строку и замените `<password>` на ваш пароль
   - Добавьте имя базы: `mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/migration_system?retryWrites=true&w=majority`

## Шаг 2: Загрузите проект на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

## Шаг 3: Деплой через Vercel

### Вариант A: Через веб-интерфейс (Проще)

1. Откройте [Vercel](https://vercel.com) и войдите
2. Нажмите **"Add New Project"**
3. Выберите ваш GitHub репозиторий
4. **Важно**: Настройте переменные окружения перед деплоем!

### Настройка переменных окружения

В разделе **"Environment Variables"** добавьте:

```
JWT_SECRET=ваш-длинный-случайный-ключ-минимум-32-символа
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/migration_system?retryWrites=true&w=majority
MONGODB_DB_NAME=migration_system
FRONTEND_URL=*
```

**Как сгенерировать JWT_SECRET:**
```bash
# Windows PowerShell:
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))

# Linux/Mac:
openssl rand -base64 32
```

5. Нажмите **"Deploy"**

### Вариант B: Через CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Задеплойте проект
vercel

# При первом деплое:
# - Link to existing project? → No
# - Project name → ваш-проект
# - Directory → .

# Добавьте переменные окружения
vercel env add JWT_SECRET
vercel env add MONGODB_URI
vercel env add MONGODB_DB_NAME
vercel env add FRONTEND_URL
```

## Шаг 4: Проверка деплоя

После деплоя Vercel выдаст вам URL, например: `https://ваш-проект.vercel.app`

1. Откройте URL в браузере
2. Зарегистрируйте первого пользователя (он автоматически станет админом)
3. Войдите в систему
4. Проверьте работу всех функций

## Важные замечания

⚠️ **Безопасность:**
- Не коммитьте `.env` файлы в Git
- Используйте сильные пароли для MongoDB
- Ограничьте Network Access в MongoDB Atlas для продакшена

📝 **После деплоя:**
- Первый зарегистрированный пользователь автоматически станет администратором
- Администраторы могут управлять ролями других пользователей
- Файлы хранятся во временной папке `/tmp` (очищается при каждом деплое)

💡 **Хранилище файлов:**
Для постоянного хранения файлов рассмотрите:
- Vercel Blob Storage
- AWS S3
- Cloudinary

## Troubleshooting

### Ошибка подключения к MongoDB
- Проверьте, что MONGODB_URI правильный
- Убедитесь, что Network Access разрешает доступ
- Проверьте username и password

### Ошибка 500
- Проверьте логи в Vercel Dashboard → Functions
- Убедитесь, что все переменные окружения установлены

### Файлы не сохраняются
- Это нормально для `/tmp` на Vercel
- Используйте внешнее хранилище для продакшена

